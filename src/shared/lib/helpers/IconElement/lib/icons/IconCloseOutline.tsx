import { type IconBaseProps } from '@react-icons/all-files'
import { IoIosClose } from '@react-icons/all-files/io/IoIosClose'

const IconUser = (props: IconBaseProps) => (
  <IoIosClose {...props} />
)

export default IconUser
