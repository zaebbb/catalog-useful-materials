import { type IconBaseProps } from '@react-icons/all-files'
import { FaUsers } from 'react-icons/fa'

const IconUsers = (props: IconBaseProps) => (
  <FaUsers {...props} />
)

export default IconUsers
