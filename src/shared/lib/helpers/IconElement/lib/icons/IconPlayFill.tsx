import { type IconBaseProps } from '@react-icons/all-files'
import { FaPlay } from 'react-icons/fa'

const IconPlayFill = (props: IconBaseProps) => (
  <FaPlay {...props} />
)

export default IconPlayFill
