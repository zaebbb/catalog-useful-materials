import { type IconBaseProps } from '@react-icons/all-files'
import { GoScreenNormal } from 'react-icons/go'

const IconScreenNormal = (props: IconBaseProps) => (
  <GoScreenNormal {...props} />
)

export default IconScreenNormal
