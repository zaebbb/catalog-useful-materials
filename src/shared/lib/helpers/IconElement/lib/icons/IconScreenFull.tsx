import { type IconBaseProps } from '@react-icons/all-files'
import { GoScreenFull } from 'react-icons/go'

const IconScreenFull = (props: IconBaseProps) => (
  <GoScreenFull {...props} />
)

export default IconScreenFull
