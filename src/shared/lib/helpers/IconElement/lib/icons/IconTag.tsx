import { type IconBaseProps } from '@react-icons/all-files'
import { CiHashtag } from 'react-icons/ci'

const IconTag = (props: IconBaseProps) => (
  <CiHashtag {...props} />
)

export default IconTag
