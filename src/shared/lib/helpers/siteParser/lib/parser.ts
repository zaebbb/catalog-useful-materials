import { type SiteParseItems, type SitePart } from '../types/SiteParserTypes'

export const parser = async (url: string, part: SitePart, item: SiteParseItems) => {
  // const parserSite = await $axiosRemote(url).get(url)
  //
  // console.log(parserSite)
}
